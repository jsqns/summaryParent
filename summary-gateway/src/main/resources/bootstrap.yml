spring:
  application:
    name: summary-gateway
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        group: DEFAULT_GROUP
        name: summary-gateway
        file-extension: yaml
